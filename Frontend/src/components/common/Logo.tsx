import { CloudLightning } from 'lucide-react';

const Logo = () => {
  return (
    <div className="flex items-center justify-center w-8 h-8 rounded-lg bg-gradient-to-br from-primary-500 to-accent-500 text-white">
      <CloudLightning className="w-5 h-5" />
    </div>
  );
};

export default Logo;